export enum MapActions {
    SET = 'set',
    DELETE = 'delete',
    CLEAR = 'clear',
}

export enum SetActions {
    ADD = 'add',
    DELETE = 'delete',
}

export enum PriceKeys {
    MIN_PRICE = 'minPrice',
    MAX_PRICE = 'maxPrice',
}